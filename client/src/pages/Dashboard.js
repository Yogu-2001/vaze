import React, { useEffect } from "react";
import { useSelector } from "react-redux";
const Dashboard = () => {
  // const user = useSelector((state) => state.auth.user);
  // const token = useSelector((state) => state.auth.token);
  return (
    <>
      <h1>Dashboard</h1>
      {/* <p>
        _id : <strong>{JSON.stringify(user?._id)}</strong>
      </p>
      <p>
        name : <strong>{JSON.stringify(user?.name)}</strong>
      </p>
      <p>
        email : <strong>{JSON.stringify(user?.email)}</strong>
      </p> */}
      {/* <p>
        token : <strong>{JSON.stringify(token)}</strong>
      </p> */}
    </>
  );
};

export default Dashboard;
